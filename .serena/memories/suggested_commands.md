# 推奨コマンド
- 体裁チェック: `npx prettier --check "**/*.md"`
- Markdown lint: `npx markdownlint **/*.md`
- リンク検証 (任意): `npx markdown-link-check -r .`
- バージョン更新前の最新化: `git fetch origin && git rebase origin/master`
- PR 作成前の確認: 見出し階層・用語一貫性・日付更新・リンク切れを重点チェック。